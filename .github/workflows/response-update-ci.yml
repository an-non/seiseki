name: Response auth revision CI

on:
  push:
    branches:
      - feature/response-update-auth-revision-20260825
    paths:
      - cloudflare/**
      - .github/workflows/response-update-ci.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cloudflare-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: cloudflare/package-lock.json
      - name: Install
        working-directory: cloudflare
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Syntax check
        working-directory: cloudflare
        run: npm run check
      - name: Phase 1-2 auth/ownership tests
        working-directory: cloudflare
        run: node --test --test-name-pattern='anonymous response|account can create|account deletion' tests/response-auth-revision.test.mjs
      - name: Phase 3 revision/queue test
        working-directory: cloudflare
        run: node --test tests/response-revision-queue.test.mjs
      - name: Required full regression suite
        working-directory: cloudflare
        run: npm test
