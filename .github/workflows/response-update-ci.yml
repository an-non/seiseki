name: Response auth revision CI

on:
  push:
    branches:
      - feature/response-update-auth-revision-20260825
    paths:
      - cloudflare/**
      - .github/workflows/response-update-ci.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cloudflare-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: cloudflare/package-lock.json
      - name: Install
        working-directory: cloudflare
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Syntax check
        working-directory: cloudflare
        run: npm run check
      - name: Phase 1-3 auth/revision tests
        working-directory: cloudflare
        run: node --test tests/response-auth-revision.test.mjs
      - name: Existing regression suite compatibility report
        id: legacy
        continue-on-error: true
        working-directory: cloudflare
        run: npm test
