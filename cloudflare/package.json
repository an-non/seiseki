{
  "name": "seiseki-cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.mjs && node --check src/db.mjs && node --check src/config.mjs && node --check src/analysis.mjs && node --check src/auth.mjs && node --check src/validation.mjs && node --check src/rate-limit.mjs",
    "test": "node --test tests/*.test.mjs",
    "diagnose:groq": "node scripts/run-groq-legacy-comparison.mjs",
    "dev": "wrangler dev --local",
    "db:migrate:local": "wrangler d1 migrations apply seiseki-db --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
