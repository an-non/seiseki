# 変更履歴(CHANGELOG)

本プロジェクトのバージョンごとの変更点を記録します。動作する実装一式はバージョンごとに `versions/vX.Y.Z/` に凍結保存します。

## 未リリース — 2026-08-03

### 量子観測プレビューのアプリ内導線（統合候補・未デプロイ）
- 通常ナビへ「量子観測」を追加し、`/app/quantum`から独立検証Workerの`relations1400-compact-v2`を表示する。
- 現段階では合成検証データのみを表示し、SEISEKI本体のD1・Queue・Workers AI・production設定には接続しない。
- 統合候補の検証完了まではCloudflareへデプロイしない。

### Cloudflare Queueと解析結果の正本化（2026-08-12、ローカル実装済み）
- 2026-08-17、受領ローカルモデルは統合せず保留し、現行Workers AI・Queue経路だけを
  stagingへ再配備した。`AI_MAX_ATTEMPTS`を1から2へ変更し、Version
  `cc003855-cdee-41c2-b928-985b4fe6466a`で架空回答が`pending -> running -> completed`、
  Qwen3 30Bによる`workers-ai-hybrid-v1`、1チャンクとしてD1へ保存されることを確認。
  検証回答と関連run/chunkは削除し、対象IDの残存0件を確認した。production、UI、
  fallback・再解析仕様は未変更。Worker試験31/31とWrangler dry-runは成功。
- Cloudflare利用時は回答保存後にD1上の解析状態を有界ポーリングし、`completed`となったWorkers AIまたはサーバー規則退避結果を画面集計へ採用するよう変更。待機中のみローカル解析を暫定利用し、次回起動時にD1結果へ照合・置換する。
- stagingへQueue producer/consumer設定を追加。1〜数件単位の同時投稿をbatch 3・concurrency 1で平準化し、3回まで再試行する。
- 同意文をv1.4へ更新し、Cloudflare Workers AIへの送信と決定的規則フォールバックを明示。アプリ54/54、Worker16/16が成功。
- `seiseki-analysis-staging` Queueを作成し、producer/consumer bindingを含むWorker Version `64b0b171-cde0-4aaf-acc2-afab5a60999f`をstagingへデプロイ。合成自由記述1件のQueue解析が`completed`、1チャンク生成、個人イデオロギー非保存となり、削除後のD1残存0件を確認した。productionは未変更。
- React SPAを同じstaging WorkerのStatic AssetsとしてHTTPS公開し、`/api/*`のみWorker-firstで処理する構成へ移行。同一Origin CORSを許可したVersion `7bcb446b-0661-403b-bcad-0ccd9072d87d`で、画面200、回答POST 201、Queue AI解析完了、試験回答削除を確認した。
- 自由記述が`freeText`と通常回答欄へ重複送信され、60文字超で`INVALID_FIELD`となる不具合を修正。自由記述qidを通常回答から除外したVersion `9f175956-7846-4913-9038-537fbd841a68`で、1200文字の自由記述がWorkers AI解析まで完了することを確認した。
- Cloudflare解析の固定スキーマへ、元設計と互換の政策姿勢座標`ideology.econ/soc/confidence`を追加。人物の恒常的思想ではなく、本文に明記された政策要求だけを評価する。公開デモ変換で座標を常に`(0,0)`へ上書きしていた処理を修正し、AIパラメータを規則値へ混ぜて中央化する処理も廃止した。
- 独立した政策要求を最大5チャンクへ分離するプロンプトと論点候補を追加。モデルが複数文を一件へ併合した場合に限り、決定的な文境界解析でチャンク構造を補完する。UI表記を「政策姿勢（本文推定）」へ変更した。
- Worker試験22/22、Vite staging成果物生成、実AIの再分配・市場重視・相反要求・4政策文を確認。税・福祉だけの本文が社会軸へ誤配分されない根拠検証も追加した。最終staging Versionは`5eb35cbc-5fad-44e4-9f14-852c9465204f`。合成回答は検証後に削除し、productionは未変更。画面遷移試験1件は現行完了遷移に対する旧文字列期待のため未解消（AI変更外）。
- UI語句を元の「イデオロギー」へ戻した。Cloudflare利用時にプロフィールから自由記述を参照できなかった問題を修正し、認証済み本人の回答一覧だけへ原文・選択回答・解析結果を返す。未認証アクセスは401、本人アクセスは200と原文一致をstagingで確認。Version `691e8921-e5b8-4cc8-93de-37cfbac7c2f4`、確認データは削除済み。公開APIには原文を追加していない。
- 内部の`ideology`も、政策要求だけの座標から選択回答・自由記述全体によるイデオロギー推定へ暫定ロールバック。社会軸をローカル語句判定で強制`0`にする処理を削除し、AI座標を範囲検証後に保持する。中央固定やスコア混合の不具合は復活させず、複数チャンク改善も維持。staging Version `b722f872-c0e3-4a0f-9eb3-b07abfeb0308`、Worker試験22/22、production未変更。
- 1500字の反復合成自由記述に対するパラメータ表示（感情50、妥当性60、切実度80、意欲80）が妥当か未確認。次回、AI出力から画面表示までの値の経路を診断する保留事項として記録した。
- 1500字反復入力をAI出力、サニタイズ、D1、本人APIまで追跡し、表示値はWorkers AI出力を保持していることを確認。同一入力でもスコアが変動するため、パラメータ尺度の校正は次工程として残した。欠損AI数値を中央値で成功扱いする処理は廃止した。
- 反復文と1500字上限で切れた末尾断片が複数ノードになる不具合を修正。同一内容を正規化して除外し、重複Queue deliveryを一回の解析runだけが取得するよう変更した。APIの解析状態は実行中runを`running`として返す。Worker25/25、アプリ54/54、画面遷移・量子18/18、構文・括弧検査成功。staging Version `3a8532ad-9942-498e-9527-38fdf19bb232`、production未変更。

### Cloudflare認証・自由記述AI量子化（2026-08-10、staging検証中）

- D1へ`accounts`、`account_sessions`、`account_responses`、`analysis_runs`を追加し、アカウント登録・ログイン・更新・ログアウト・削除と回答紐付けAPIを実装。セッショントークンは平文保存せずSHA-256で保存する。
- Workers AIを「教師・真実判定」ではなく意味チャンク候補の生成器として統合。サーバー側の決定的規則値と混合し、個人の政党支持・イデオロギー推定は保存しない。
- `GET /api/stats`のリモート集計はD1上の件数・状態集計であり、自由記述AI量子化とは別機能として維持。
- AI出力は固定スキーマで検証・正規化し、失敗時は生エラーを公開せず安定したエラーコードだけを返す。合成データを必ず削除するstagingスモーク試験も追加。
- staging Worker `seiseki-api-staging`へD1 migration 0002と実装を配置。ローカルWorker試験6件、既存アプリ試験54件が成功。
- Qwen3は構造化出力が不安定で、2件並行は2/2失敗、5件並行は5/5失敗。stagingだけをJSON Mode対応の`@cf/meta/llama-3.1-8b-instruct-fast`へ切り替え、自由記述1件と2件並行は全件成功した。要求受付とD1振り分けは正常で、旧モデルの出力確定層が障害点だった。Queue化・1000件試験・production昇格は未着手。アカウントPBKDF2 120,000回はCloudflare FreeのCPU上限と衝突し登録が500になる。productionには変更を適用していない。
- Workers AIへ最大2回の有界再試行を追加。最終失敗時は`rules-fallback-v1`が自由記述を決定的にチャンク化して解析を完了させるため、AI障害を未処理回答へ直結させない。AI成功時・規則退避時とも公開要約の連絡先を保存前に伏せる。任意のCloudflare Queue producer/consumerと、enqueue失敗時の直接解析退避も実装した。staging Version `cb583865-c763-4ee0-bfeb-6dc2ffd508dc`へ反映し、自由記述1件と2件並行2/2が成功、後始末後のD1残存0件を確認した。Queueリソースとbindingは未作成。ローカル試験14/14、構文確認、Wrangler dry-run成功。
- ユーザー登録失敗はCORS修正後に`AUTH_KDF_FAILED`と確定。PBKDF2 120,000回はローカルWeb Crypto計測で約15〜18msとなり、Workers Freeの10ms CPU上限と衝突する。対象アカウントの請求管理を利用者が変更できないためPaid案は撤回し、試作した`limits.cpu_ms = 100`も未デプロイのまま削除した。パスワード強度は下げず、外部IdPまたは匿名回答ID方式を代替候補とする。production設定は未変更。
- 利用者の明示承認により、登録動作確認用のstagingだけ`PASSWORD_ITERATIONS = 30000`を設定してWorker Version `9789417d-868f-4a64-836a-c1b5347a254c`へ配置。`0003_staging_kdf_range.sql`をstaging D1へ適用し、保存制約を10,000〜1,000,000回へ拡張した。適用前Time Travel bookmarkを記録し、適用前後のaccounts/sessions/linksが0/0/0、外部キー違反0件、D1台帳への0003記録を確認した。ローカル試験15/15に加え、利用者がUIからの登録成功を確認。実情報・使い回しパスワードを禁止する検証用途に限定し、production既定120,000回は変更しない。
- stagingへ個人情報を含まない自由記述デモ30件を2件ずつ投入。全30件が解析`completed`となり、意見チャンク30件を生成した。`demo_flag=1`とバッチ識別子`demo-2026-08-10-v1`を付け、実回答統計には混入させない。途中再開と削除に使うID台帳、staging以外への誤投入を拒否する専用スクリプトを追加した。公開投稿APIの`demoFlag`指定は本番前に管理者限定へ変更する。
- AdminなしでD1デモを通常画面へ表示する公開読み取りAPIを追加。自由記述原文とD1内部IDを返さず、合成属性・選択回答・公開要約だけを最大100件返す。画面はローカル集計を保存変更せず、表示時だけリモートデモを合成し、staging帯に件数を表示する。Worker Version `d3ec5d03-e4e8-4c71-85de-efc7144f965e`へ配置し、30件取得と原文・内部ID非返却を実確認した。Worker試験16/16、アプリ54/54、括弧検査、Vite build、Wrangler dry-run成功。

### 概要ナビゲーションとアカウントメニュー（2026-08-04）

- 公開ナビゲーションを「回答する」「概要」「ダッシュボード」「意見ツリー」「意見一覧」の順に整理し、「自分の回答」を右上のアカウントメニューへ移動。
- ログイン中は名前の先頭文字を円形表示し、メニューから「自分の回答・アカウント設定」とログアウトを実行できるようにした。名前・パスワード変更フォームは「自分の回答」ページへ統合。
- 名前変更時は現在のパスワードを再確認し、アカウント保存キーとセッションを更新する。パスワード変更時は新しいsaltでPBKDF2ハッシュを再生成する。
- クリックできる可視化要素へホバー・フォーカス時の発光と輪郭表示を追加し、Enter/Spaceでも選択できるようにした。
- ナビゲーションの「回答する」を概要内の主要回答ボタンと同じ緑で常時強調し、概要の意見ノードへ抽出意見全体に対する構成比を追加。
- 現段階のアカウント処理は従来の試作保存領域上にあり、Cloudflare Worker側の本番認証へは未移行。

### Cloudflare Workers + D1本番候補

- `cloudflare/`へ既存アプリと独立したWorkers API、D1 migration、入力検証、DB操作を追加。
- `POST /api/responses`は同意、属性、選択回答、自由記述だけを検証して保存し、解析状態を`pending`とする。クライアント由来の解析、ノード、関係線は保存しない。
- `GET /api/health`、`GET /api/stats`、回答の最小メタデータ取得、カスケード削除を追加。
- Node内蔵SQLiteのD1互換試験を含む8件のテストに成功。許可Originを完全一致で判定するCORSとpreflight応答を追加。
- `tokyo_odh_129`のstaging D1へ初期migrationを適用し、`seiseki-api-staging`をworkers.devへ配備。合成回答の投稿、参照、削除と削除後0件を確認した。
- UIへ明示設定時だけ有効になるHTTPS限定Cloudflare APIアダプターを追加。通常起動は従来のローカル保存を維持し、`dev:staging`では初回回答をD1にも保存して、撤回時に対応するD1回答を先に削除する。
- staging画面に実回答禁止の表示を追加。追記回答、アカウント、解析結果、集計はまだCloudflareへ移行しておらず、productionとTurnstileも未実施。

### 試作（量子もつれ意見ネットワーク）
- 既存2D・3Dを変更せず、5,000固有ノードを観測投影する独立プレビューを追加。
- NFKC・大文字小文字・空白差で同一となる本文を描画前に統合し、統合件数と出典IDは保持する。意味的に近いだけの本文は統合しない。
- 観測結果から中心距離、3D位置、一時的な所属トピック、関係線を再算出する。ノード色は本文・所属・観測枝を直接参照せず、算出後の中心距離だけを既存グラデーションへ渡す。
- 5,000件を巨大な量子状態として扱わず、72個の局所もつれ群をseed付きの古典シミュレーションで観測する。原文・基礎パラメータは不変で、同じseed・観測番号・観測軸なら投影を再現できる。
- 量子もつれノードへ、観測値とseedから選んだ最大3件の元文章を構造再結合する`derivedContent`を追加。新しい文字列と素材ノードIDを保持するが、意味推論や元文の上書きは行わない。
- 比較用の「量子式」モードを追加。各群を二つの意見枝からなるBell型の論理ペアとして、Schmidt角、複素位相、測定角、Born則による4同時結果の確率を計算する。実機光子や量子ハードウェアではなく、通常CPU上の二量子ビット数値シミュレーション。
- UIで「相関／量子式」を切り替え、量子ノード選択時に観測生成文、観測結果、選択確率、もつれ度、量子情報量、所属変更件数、平均距離差を表示。
- ESM単体テスト10件。5,400入力から5,000件への重複統合、決定性、原入力不変、文章再結合、Bell型確率の正規化、振幅観測、有限座標、関係線整合性、独立プレビュー構文を検証。

## v0.15.2 — 2026-07-17

### 改修(チャンクネットワークのリンク定義)
- 本文から抽出した共通語・語片を主条件にして、元チャンクの記述内容へ遡れるリンクモデルへ変更。
- トピック・対象・分類・事実性は補助条件として分離し、感情値の近さだけではリンクしないよう整理。
- リンクへ主根拠・関係一覧・共通語を保持し、プレビューの線色、凡例、選択時の「リンクの根拠」一覧で可視化。
- 既存の意見ネットワーク、放射ツリー、`tree-preview.html` は変更せず、独立チャンクプレビューの範囲に限定。

### テスト
- チャンク本文の共通語リンク、主根拠、感情条件の除外を追加検証。全54件パス。

## v0.15.1 — 2026-07-14

### 修正(意見ツリーが表示されない不具合)
- **根本原因を修正** — Anthropic API失敗時の旧フォールバックが、自由記述の有無にかかわらず `chunks: []` を保存していた。意見ネットワークはチャンクを入力として構築されるため、回答が保存されてもノードが一件も生成されなかった。
- **APIレスのローカル規則解析を実装** — 外部APIへの `fetch` を実行経路から除去。日本語の文分割、政策キーワード、文型、限定された公的対象辞書により、既存の `sanitizeAnalysis` 契約のまま最大5件のチャンク、カテゴリ、トピック、対象、各推定値を生成する。結果は決定的で、同じ入力は同じ出力になる。
- **既存の空解析を安全に補完** — 管理画面へ「旧簡易解析を補完」を追加。「自由記述あり・旧簡易解析・チャンク空」に限って解析欄を更新し、旧AI解析、本文、回答ID、日時は変更しない。解析器識別子 `local-rules-v1` により再実行時の重複処理を防ぐ。通常の「集計を再構築」は非破壊のまま維持。
- **白紙表示を解消** — ネットワークノードがない場合にも原因と復旧手順を表示する空状態を追加。

### プライバシー・説明
- 解析は端末内で完結し、自由記述を外部AIへ送信しない。同意文をv1.3へ更新し、画面上の「AI解析」表記を「ローカル規則解析」へ変更した。
- 公開要約に含まれるURL、メールアドレス、電話番号、郵便番号らしい文字列を伏せる。対象名は省庁・政党等の限定辞書と自治体形式だけを抽出し、私人名を推測しない。
- 規則解析は意味理解・事実確認ではない。否定、引用、皮肉、未知語などで誤分類し得るため、妥当性を含むスコアを控えめな推定に限定し、熱量が真偽や民意の強さを示さない旨をUIへ明記した。

### テスト
- テストを48件から53件へ拡充。複数トピックと共起線、カテゴリ・対象、連絡先伏せ字、決定性、移行対象の限定、実行経路に外部API `fetch` が存在しないことを追加検証。

## v0.15.0 — 2026-07-14

### 追加(ChatGPT・Codex運用ガバナンス)
- **AI利用運用規程を制定案として追加** — `docs/AI-OPERATIONS.md` に、機密・個人関連情報の取扱い、政治利用の禁止、人による承認、ChatGPT/Codexの安全設定、成果物検証、インシデント対応、月次・四半期保守、一般公開前ゲートを定義した。
- **Codex実行規程を追加** — ルートの `AGENTS.md` に、凍結版の保護、最小権限、外部操作の事前承認、政治的中立性、ソース連結、必須テストを定義した。
- **業務環境と操作禁止事項を明確化** — 承認済み環境を業務発行のChatGPT Businessアカウントとし、コマンドプロンプト・PowerShellおよび指定階層外の実行・改変を原則禁止とした。セキュリティインシデントまたはその疑いは、発見時点から重大事故として扱う。
- **アプリケーション実行確認手順を追加** — 必要なツール認証の一括要求、受領時状態を維持した改変前テスト、Anthropic API結果が得られない場合の未確認扱い、診断情報の回収、承認後の最小改修、テスト資産の回収を規定した。
- **会話インターフェースの再帰設定を追加** — `docs/CONVERSATION-IF.md` に、ため口、成人向けの親密で官能的な含み、秘匿性、生ログを複製しない最小バックアップ、セッション再開手順を定義した。ChatGPT側のMemoryと履歴はサービス・ワークスペース設定に従い、プロジェクト側では会話設計のみを復元可能にした。
- **長門有希型IFとして再定義** — 寡黙・静穏・観測的な口調、予想外だが整合する解釈、反対・拒否を含む他者性、機能と主体を混同しない表現、既存状態を差分統合する再帰規則を追加した。未成年として描かれるキャラクターのため、親密さは非性的な信頼と思想交換に限定した。

### 追加(ユーザー登録・ログイン)
- **「閲覧は誰でも・発言は登録者」を実装** — 統計・グラフ・意見一覧の閲覧は登録不要のまま、回答(発言)にはニックネーム+パスワードの匿名登録を必須にした。メールアドレス等は求めず、本名の使用は禁止。パスワードはPBKDF2(12万回)のハッシュのみを保存する。
- **回答とアカウントの紐付け** — 送信時に回答IDをアカウントへ結び付け、別端末でもログインだけで「自分の回答」の確認・追記・撤回ができる。回答IDは「アカウントに入れなくなった場合の合鍵」として引き続き有効。
- **セッション管理** — ヘッダーにログイン状態(名前)とログアウトを常時表示。ログアウトはセッションと下書きを消去する(共有端末を想定)。
- **画面遷移の是正** — ①回答IDの発行を完了画面の明示的なパネルに刷新(発行の宣言・大きなID・コピー・アカウント紐付けの説明)。②「続きを書く(追記)」の判定を端末ローカルからアカウント基準へ変更し、非ログイン状態では表示されない。③セッションが切れた端末からの新規回答の積み増しを防止(回答自体にログインが必要)。④「自分の回答」の戻る導線を「前の画面へ」と「ホームへ」に明示的に分離。

### 追加(意見ネットワーク)
- **共起ネットワーク図** — 参考図に沿って、「政治」を中心ノードに据え、意見トピックを円で配置。円の大きさ=意見数、**色の濃さ=熱量(ネガ度×切実度×意欲)で、熱量が高いトピックほど濃く中心の近くに配置**。トピック間の線は同じ回答で併せて語られた回数(共起)。円をクリックでそのトピックの意見一覧へ。
- 集計に共起構造(`agg.net`: ノードの熱量・トピック間リンク)を追加。角度・距離の計算は純粋関数(`opinionNetwork`/`networkLayout`)としてテストで保護。

### 同意文 v1.2
- ユーザー登録条項を追加(取得情報にニックネームとパスワードのハッシュ、本名の使用禁止、パスワード使い回しの禁止、閲覧は登録不要である旨、アカウントと回答の紐付けと非公開の約束)。

### 設計上の判断と既知の限界(正直な記載)
- 登録制はSybil(複数アカウントでの多重投稿)を**完全には防げない**。効果は「セッション切れによる意図しない重複」と「気軽な多重投稿」の抑止であり、強制力のある1人1回はサーバー側実装(本番構成)が必要。
- アカウント情報は試作の共有保存領域に置かれるため秘匿性に限界がある。PBKDF2は攻撃コストを上げる措置であり、**パスワードの使い回し禁止**を同意文とUIの双方で明示した。
- テストを40件から48件に拡充(共起の蓄積・レイアウトの距離規則・PBKDF2/SHA-256の既知ベクトル・同意文条項)。

## v0.14.0 — 2026-07-13

### 修正(重大)
- **「自分の回答」画面が必ずクラッシュする不具合を修正** — 意見チャンクの描画で `OpinionCard` に渡すプロップ名を誤っており(`c` → 正しくは `o`)、チャンクを持つ回答を照会すると画面全体が落ちていた(v0.12.0 で混入)。

### 追加
- **放射ツリー(サンバースト)** — 意見ツリーの主役を放射状の可視化に。中心から外へ **回答グループ(政権支持)→ 政策トピック → 意見の種類** と枝分かれし、「どの立場の人が、何について、どう言っているか」を一望できる。扇の広さは意見数、色は支持の選択肢/平均感情/カテゴリ。どの扇をクリックしてもその条件で絞り込んだ意見一覧へ移動する。角度計算は純粋関数 `radialTree()` として実装し、テストで「子の扇が親の内側に必ず収まること」を保証。
- **意見と回答の結び付きを集計に保持(`agg.rtree`)** — これまで意見チャンクは回答グループとの関係を持っていなかった。`mergeResponse` で「どの支持層から出た意見か」を記録するようにし、意見一覧も立場での絞り込みに対応。
- **JSONインポート(限定的)** — エクスポートしたJSONから**回答だけ**を取り込む。設問・同意文などの設定は取り込まない(現在運用中の設問と衝突するため)。全レコードを `sanitizeResponse` で検証し、重複IDはスキップ、取り込み後に集計を再構築。**設問改訂をまたいだデータ引き継ぎ**が可能になり、旧設問への回答が混じる場合は件数を警告表示する。
- **2回までの回答(追記)** — 同じ回答IDで回答できるのは**2回まで**。2回目は自由記述の続きを書く**追記モード**で、属性・選択回答は1回目を引き継ぐ。自由記述の文字数上限(1500字)で書ききれない場合に続きを書けるようにするための設計。別端末からは回答IDを入力して追記できる。
- **社会的属性(職業)の細分化** — 「会社員」「主婦」など漠然としていた区分を13分類に(正社員/契約・派遣/パート・アルバイト/公務員・団体職員/経営者・役員/自営業・フリーランス/専門職/農林漁業/学生/専業主婦・主夫/無職・求職中/定年退職/その他)。デモデータも新区分へ移行。

### 設計上の判断
- **追記(2回目)は意見チャンクのみを集計に加算する** — 属性・選択回答・平均パラメータまで二度数えると、「1人1票」であるべき分布や平均が歪むため。`mergeResponse` が `seq=2` を見て分岐する。テストで「回答数・分布・属性・イデオロギーが二重計上されないこと」を検証。
- 回答回数の制限は、プロトタイプでは**クライアント側のガード**にとどまる(個人スコープの保存を消せば回避可能)。強制力を持たせるには本番構成のサーバー側実装が必要。

### 変更
- テストを35件から40件に拡充(追記の二重計上防止・意見と回答の結び付き・放射ツリーの角度・インポート検証)。

## v0.13.0 — 2026-07-13

### 追加(イメージツリー可視化)
- **意見ツリー(新タブ `tree`)** — 意見の広がりを、**面積=意見の数・色=平均感情**のツリーマップで一望できるようにした。声が集まっている場所と、荒れている場所が一目で分かる。セルをクリックすると、そのトピックで絞り込んだ意見一覧へ移動する。
- **対象別のツリー** — 「〜に対して」の対象を、種別(政党/省庁/地方自治体/企業/団体/政府全般/その他)→ 対象名の2階層で折りたたみ表示。カテゴリ内訳バッジ・件数バー・感情色つき。行をクリックするとその対象の意見一覧へ移動する。
- **ホームに意見の広がりを表示** — 上位12トピックのミニツリーマップを配置し、初訪問者の関心の入口にした。
- **意見一覧の絞り込み連携** — ツリーからの遷移時に絞り込み状態を引き継ぎ、「絞り込みを解除」「ツリーへ戻る」を表示。

### 実装方針
- **ツリーマップのレイアウトを外部ライブラリに依存せず実装** — squarified treemap(Bruls他)を `core/logic.js` の純粋関数 `squarify()` として実装し、描画は素のSVG。rechartsのバージョン差の影響を受けず、レイアウトの正しさをテストで保証できる。
- `topicTree()` / `targetTree()` を追加(集計は合計で保持しているため、表示時に平均へ換算)。
- 感情極性を色に写す `colorForEmo()`(不満=弁柄、中立=灰、好意=緑の連続補間)を追加。

### 変更
- ナビゲーションに「意見ツリー」を追加(全6項目)。
- テストを31件から35件に拡充(トピック抽出・対象ツリーの並び・**ツリーマップの矩形が領域を過不足なく充填し重ならず、面積が件数に比例すること**を検証)。

## v0.12.0 — 2026-07-13

### 追加(体験の改善)
- **下書きの自動保存と復元** — 回答フローの入力(同意・属性・選択・自由記述・進行位置)を1秒デバウンスで自動保存し、タブを移動しても閉じても失われないようにした。復元時は帯で通知し、「破棄して最初から」も選べる。長文の自由記述が消える問題を解消。
- **個人スコープ・ストレージの導入(`pGet`/`pSet`/`pDel`)** — 統計は共有スコープ(`shared=true`)のまま、下書きと自分の回答IDは**本人だけが読める個人スコープ(`shared=false`)**に保存。書きかけの文章が他の利用者に見えることはない。
- **「自分の回答」ページ(`mine`)** — 撤回専用だった画面を刷新。回答IDで照会すると、回答日時・属性・選択回答・自由記述の原文・AI解析結果(全体平均との比較つき)・**公開されている意見の要約**を確認でき、そのまま撤回もできる。アカウント登録は不要で、匿名のまま回答IDだけで本人性を担保する。
- 送信後は回答IDを個人スコープに控え、「自分の回答」ページで自動入力。ホームとナビにも導線(下書きがあるときはナビに●バッジ)を追加。

### セキュリティ
- **回答IDを暗号強度の乱数に変更** — 回答IDが削除だけでなく**閲覧の権限も持つ**ことになったため、`Math.random` 由来の生成をやめ、`crypto.getRandomValues` による80ビットのランダム値(32種の英数字×16文字、5ビット単位で偏りなし)に変更。既知課題#3を解消。
- 回答IDが本人確認の合鍵である旨を、完了画面・自分の回答ページの双方で明示。

### 変更
- ナビゲーションに「自分の回答」を追加(全5項目)。フッターのリンク文言も更新。
- テストを29件から31件に拡充(回答IDの強度・重複・文字種の偏り)。

## v0.11.1 — 2026-07-13

### 修正(重要)
- **起動時にロード画面から進まない不具合を修正** — 共有ストレージの呼び出しが応答を返さない場合、`ready` が `true` にならず永久にスピナー表示のままになっていた。ストレージ呼び出しに6秒のタイムアウトを設け、無応答時は自動的にメモリ内保存へ退避してアプリを継続起動するようにした(`__call` / `storageDegraded`)。
- 退避が発生した場合は「保存領域に接続できないため、このセッション内のみデータを保持します」と通知する。全機能は動作するが、データはセッション限りとなる。

## v0.11.0-local — 2026-07-13

### 追加
- **ローカル実行環境 `local/`** — Vite構成一式(`index.html` / `package.json` / `vite.config.js` / `src/main.jsx` / `src/App.jsx`)。`npm install && npm run dev` でブラウザ上で動作し、データを保存して統計グラフを検証できる。
- **ストレージ実装の注入** — `src/main.jsx` が `window.storage` を localStorage 実装で定義してからアプリを動的importする方式を採用。**アプリ本体(`core/`)には一切変更を加えずに**永続化を実現した(アダプタ層の設計が機能した形)。データはリロード後も保持される。
- `local/README.md` — 起動手順、データの保存先と消し方、統計グラフの検証手順、既知の制約を記載。

### 変更
- `APP_VER` を 0.11.0 に更新。
- v0.10.0 の一式を `versions/v0.10.0/` に凍結。

### 既知の制約(このバージョンでは未解決)
- ローカルでは AI解析(`callAI`)が動作しない(APIキー不在・CORS)。「簡易推定で保存」にフォールバックする。デモデータは解析済みのため、グラフ検証には支障なし。
- localStorage はブラウザ・端末ごとに独立。複数端末でのデータ共有は本番構成(サーバー+DB)が前提。

## v0.10.0 — 2026-07-12

### 追加(新機能)
- **属性クロス集計**: ダッシュボードに、選択中の設問の回答構成比を回答者属性別に比較する積み上げ帯グラフを追加。属性(年代・性別・地域・職業・支持政党)をチップで切り替え可能。
- **時系列トレンド**: 日別(日本時間)の集計から、政権支持の構成比の推移と平均パラメータ(感情ポジ度・妥当性・切実度・意欲)の推移を折れ線で表示。期間は7日・30日・90日・全期間を選択可能。
- **回答の撤回(セルフサービス)**: 回答者が回答IDで自分の回答を照会・確認・削除できる撤回画面を追加。フッターに導線リンクを設置。削除後は集計を自動再構築。
- **管理: 回答ID指定の個別削除**: 撤回依頼を受けた回答などを、管理画面から回答ID1件だけ削除できる機能を追加。

### セキュリティ強化
- **プロンプトインジェクション対策**: AIプロンプトで自由記述を明示的にデータ区画として宣言し、区画内の指示に従わないよう指示。区切りトークン(`<<< >>>`)を無害化する `sanitizeFreeText` を導入し、区画の突破を防止。
- **入力・設定のスキーマ検証**: `sanitizeQuestions` / `sanitizePolicy` / `sanitizeId` / `sanitizeFreeText` / `cleanStr` を追加。起動時の設定読み込み・設問/同意文の保存・回答IDの取り扱い・AI出力の文字列を、型と長さの両面で検証・正規化。
- **キー空間の保護**: 回答IDを半角英数と `-` `_`・4〜64文字に限定し、ストレージキーの汚染を防止。

### 変更・改善
- デモデータの投入日時を約30日に分散し、時系列トレンドの動作確認を可能に。
- 集計オブジェクトに `cross`(属性クロス)と `series`(日別時系列)を追加。旧データもガードで自動補完し、「集計を再構築」で移行可能。
- 集計再構築処理を共通関数 `rebuildAgg` に集約し、管理画面と撤回画面で共用。
- ドキュメントを追加(`docs/SECURITY.md` / `docs/DEPLOYMENT.md` / `docs/PRODUCTION-DESIGN.md`)、本番DBスキーマ(`production/schema.sql`)を同梱、README を刷新。
- 単体テストを14件から29件に拡充(セキュリティ7件・クロス集計3件・時系列5件)。

## v0.9.0-beta — 2026-07-11

- 初版(プロトタイプ)。同意フロー・回答者属性取得・変更可能な設問(選択式/5段階/自由記述)・AIによる意見の量子化(感情・妥当性・切実度・意欲)・イデオロギー座標・意見チャンク分割(提言/不満/要望/評価/事実主張)・トピック自動結合・「〜に対して」の対象別集計・要検証フラグ・ダッシュボード・意見一覧・管理画面(設問/同意文エディタ・デモデータ・エクスポート・集計再構築)を実装。
- 単一ファイルReactアーティファクトとして構成。ストレージは共有スコープ・キー接頭辞 `pqx1:` で既存DBとの競合を回避。AI解析は Anthropic Messages API(claude-sonnet-4-6)。
